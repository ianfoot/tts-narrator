name: tts_narrator_workspace
description: "Pub workspace for the OpenRouter TTS narration CLI + macOS GUI."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.13.1

workspace:
  - app
  - packages/core
  - packages/cli
  - packages/providers/openrouter